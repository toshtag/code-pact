id: P1
name: Foundations
weight: 20
confidence: high
risk: low
status: done
objective: >
  Establish core schemas (Roadmap, Phase, Task, ProgressEvent), the pack pipeline
  that renders context packs from design files, and the five MVP CLI commands:
  init, phase, progress, pack, verify.
definition_of_done:
  - All five MVP commands functional
  - All commands support --json
  - Schema validation with zod
  - Unit + integration test suite green
verification:
  commands:
    - pnpm test
    - pnpm typecheck
    - pnpm build
tasks:
  - id: P1-T1
    type: feature
    ambiguity: low
    risk: low
    context_size: medium
    write_surface: high
    verification_strength: strong
    expected_duration: long
    status: done
    description: Core schemas and pack pipeline (Roadmap, Phase, Task, ProgressEvent, buildContextPack)
  - id: P1-T2
    type: feature
    ambiguity: low
    risk: low
    context_size: medium
    write_surface: high
    verification_strength: strong
    expected_duration: long
    status: done
    description: Five MVP CLI commands (init, phase, progress, pack, verify) with --json support
