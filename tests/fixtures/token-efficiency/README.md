# Token-efficiency fixtures

This directory is intentionally empty. The closed harness in
`tests/harness/token-efficiency/` generates deterministic temp projects and
fixtures at runtime, so tracked fixtures are not required.
