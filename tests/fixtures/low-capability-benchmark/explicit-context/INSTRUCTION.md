# Generic Agent Instruction

Implement the requested behavior. The full specification is not in this
instruction; retrieve it from the `context/` directory before changing code.
Use only the allowed writes and the provided verification commands.
