// Type declarations for release-notes.mjs (consumed by the unit test;
// the script itself runs as plain Node ESM).

export function extractReleaseNotes(changelog: string, version: string): string | null;
