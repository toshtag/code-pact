# History-noise gate allowlist — scripts/check-history-noise.mjs
#
# Each non-comment line is a repo-relative path EXEMPT from the version-tag
# (vX.Y) gate.
#
# RATCHET. The checker MECHANICALLY rejects a "stale" entry — an allowlisted
# file that no longer contains any version tag — so cleaning a file forces its
# removal from this list. Adding a new entry to silence a fresh violation is
# NOT blocked by the checker; it is forbidden by review — clean the file
# instead. In practice the list only shrinks. docs/maintainers/** is exempt in
# the checker itself (the sanctioned home for version history) and is not listed.

# --- Permanent: version numbers are load-bearing to the doc's purpose ---
docs/upgrading.md
docs/migration.md
# cli-contract.md is the versioned CLI *contract* reference: its "Stable (v1.0)"
# / "(v1.6+)" annotations are availability/compatibility information (which
# version a command, flag, or guarantee is available from) — like a published API
# reference, not drift-prone history. Same rationale as upgrading/migration.
# Permanently exempt; correctness bugs are fixed in place, never by stripping the
# version annotations.
docs/cli-contract.md

# --- Temporary: known-dirty docs, to be cleaned/collapsed and de-listed ---
docs/spec-kit-bridge.md
