node_modules/
.env
.env.local
dist/
build/
.DS_Store
*.tsbuildinfo

# Root-level tool state (anchored with / so tests/fixtures/**/.code-pact/ is not affected)
/.code-pact/

# Root-level generated context output
/.context/

# Root-level adapter-generated files (per-developer state, consistent with
# /.code-pact/ being gitignored — see CHANGELOG v0.9.0 release-prep note).
/CLAUDE.md
/AGENTS.md
/GEMINI.md
/.claude/
/.cursor/

# Generated context inside test fixtures (written by pack tests)
tests/fixtures/**/.context/

# Root-level private design notes (planning, strategy, retro)
/.local/
*.tgz

# Per-maintainer SSH allowedSigners for `git verify-tag`. Intentionally
# not committed in v0.2 (single-maintainer); see CONTRIBUTING.md.
.git_allowed_signers
